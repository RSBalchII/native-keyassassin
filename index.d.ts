/**
 * Removes JSON artifacts, escape characters, and control sequences from the input text.
 * @param text The input text to cleanse
 * @returns The cleansed text with JSON artifacts removed
 */
export function cleanse(text: string): string;